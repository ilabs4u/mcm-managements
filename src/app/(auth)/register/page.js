import RegisterForm from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Register — MCM Franchise',
  description: 'Register your franchise outlet with the MCM management system.',
};

export default function RegisterPage() {
  return <RegisterForm />;
}

